<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MSP CTF | Challenges</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar animate-fade-in">
    <div class="nav-brand">
      <img src="/images/logo.png" alt="MSP Logo" class="nav-logo">
      <span>MSP CTF</span>
    </div>
    <div class="nav-links">
      <a href="/challenges" class="nav-link active">Challenges</a>
      <a href="/scoreboard" class="nav-link">Scoreboard</a>
      <% if (user.role === 'admin') { %>
        <a href="/admin" class="nav-link" style="color: var(--msp-yellow)">Admin</a>
      <% } %>
      <a href="/logout" class="nav-link" style="color: var(--msp-orange)">Logout</a>
    </div>
  </nav>

  <div class="container animate-fade-in delay-1">
    <header style="margin-bottom: 1rem; text-align: center;">
      <h1>Challenges</h1>
      <h3 style="color: var(--msp-orange); text-transform: uppercase; letter-spacing: 1px; font-size: 1rem; margin-bottom: 0.5rem;">Cybersecurity Red Team</h3>
      <p style="color: var(--text-secondary);">Solve puzzles, exploit vulnerabilities, and capture flags.</p>
    </header>

    <% if (message) { %>
      <div class="alert alert-success"><%= message %></div>
    <% } %>
    <% if (error) { %>
      <div class="alert alert-error"><%= error %></div>
    <% } %>


    <h2 style="margin-top: 2rem;">Available Challenges</h2>
    <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 2rem;">
      <% availableChallenges.forEach((challenge, index) => { 
          const isSolved = solvedSet.has(challenge.id);
      %>
        <div class="glass-card animate-fade-in <%= isSolved ? 'border-solved' : 'border-unsolved' %>" style="--anim-delay: <%= (index * 0.1) + 0.2 %>s; animation-delay: var(--anim-delay);">
          <div style="display: flex; justify-content: space-between; align-items: start; margin-bottom: 1rem;">
            <h3 style="margin-bottom: 0;"><%= challenge.title %></h3>
            <span class="badge <%= isSolved ? 'badge-solved' : 'badge-unsolved' %>">
              <%= isSolved ? 'Solved' : 'Unsolved' %>
            </span>
          </div>
          
          <div style="display: flex; gap: 0.5rem; margin-bottom: 1rem; flex-wrap: wrap;">
            <span class="badge bg-points"><%= challenge.points %> PTS</span>
            <span class="badge bg-category"><%= challenge.category || 'Misc' %></span>
          </div>
          
          <p style="color: var(--text-secondary); margin-bottom: 1.5rem; line-height: 1.5;">
            <%= challenge.description %>
          </p>

          <div style="display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 1.5rem;">
            <% if (challenge.link) { %>
                <a href="<%= challenge.link %>" target="_blank" class="btn btn-secondary" style="padding: 0.4rem 0.8rem; font-size: 0.9rem; text-decoration: none; display: inline-flex; align-items: center; gap: 0.5rem;">
                  <span>Visit Challenge</span>
                  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path><polyline points="15 3 21 3 21 9"></polyline><line x1="10" y1="14" x2="21" y2="3"></line></svg>
                </a>
            <% } %>

            <% if (challenge.file) { %>
                <a href="<%= challenge.file %>" class="btn btn-secondary" style="padding: 0.4rem 0.8rem; font-size: 0.9rem; text-decoration: none; display: inline-flex; align-items: center; gap: 0.5rem;" download>
                  <!-- Basic icon or text -->
                  <span>Download File</span>
                  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg>
                </a>
            <% } %>
          </div>

          
          <% if (!isSolved) { %>
            <form action="/submit-flag" method="POST">
              <input type="hidden" name="challengeId" value="<%= challenge.id %>">
              <div style="display: flex; gap: 0.5rem;">
                <input type="text" name="flag" placeholder="MSP{flag_here}" required style="padding: 0.6rem;">
                <button type="submit" class="btn btn-primary" style="padding: 0.6rem 1rem;">Submit</button>
              </div>
            </form>
          <% } else { %>
             <div style="padding: 0.8rem; border-radius: var(--radius); text-align: center; font-weight: 600;" class="bg-captured">
               Flag Captured!
             </div>
          <% } %>
        </div>
      <% }); %>
    </div>

    <% if (endedChallenges.length > 0) { %>
    <h2 style="margin-top: 4rem; color: var(--text-secondary);">Ended Challenges</h2>
    <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 2rem;">
      <% endedChallenges.forEach((challenge, index) => { 
      %>
        <div class="glass-card animate-fade-in ended-challenge border-ended" style="--anim-delay: <%= (index * 0.1) + 0.2 %>s; animation-delay: var(--anim-delay);">
          <div style="display: flex; justify-content: space-between; align-items: start; margin-bottom: 1rem;">
            <h3 style="margin-bottom: 0;"><%= challenge.title %></h3>
            <span class="badge bg-ended-label">ENDED</span>
          </div>
          
          <div style="display: flex; gap: 0.5rem; margin-bottom: 1rem; flex-wrap: wrap;">
            <span class="badge bg-ended-label"><%= challenge.points %> PTS</span>
            <span class="badge bg-category"><%= challenge.category || 'Misc' %></span>
          </div>
          
          <p style="color: var(--text-secondary); margin-bottom: 1.5rem; line-height: 1.5;">
            <%= challenge.description %>
          </p>
          
          <div style="padding: 0.8rem; border-radius: var(--radius); text-align: center; font-weight: 600;" class="bg-ended-msg">
            Challenge Ended
          </div>
        </div>
      <% }); %>
    </div>
    <% } %>
  </div>

  <script src="/background.js"></script>
</body>
</html>
