<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MSP CTF | Login</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body style="justify-content: center; align-items: center;">

  <div class="glass-card animate-fade-in" style="width: 100%; max-width: 400px; text-align: center;">
    <div class="mb-2">
      <img src="/images/logo.png" alt="MSP Logo" style="width: 80px; height: 80px;">
    </div>
    <h1 style="font-size: 1.8rem; margin-bottom: 0.5rem;">MSP CTF Platform</h1>
    <h2 style="font-size: 1.2rem; color: var(--msp-orange); margin-bottom: 0.5rem; text-transform: uppercase; letter-spacing: 2px;">Cybersecurity Red Team</h2>
    <p style="color: var(--text-secondary); margin-bottom: 2rem;">Login to start hacking</p>

    <% if (error) { %>
      <div class="alert alert-error">
        <span>⚠</span> <%= error %>
      </div>
    <% } %>

    <form action="/login" method="POST">
      <div class="form-group delay-1 animate-fade-in">
        <label for="username">Username</label>
        <input type="text" id="username" name="username" placeholder="Enter your username" required>
      </div>
      
      <div class="form-group delay-2 animate-fade-in">
        <label for="password">Password</label>
        <input type="password" id="password" name="password" placeholder="Enter your password" required>
      </div>

      <button type="submit" class="btn btn-primary delay-3 animate-fade-in" style="width: 100%; margin-top: 1rem;">
        Log In
      </button>
    </form>
    
    <div class="mt-2" style="font-size: 0.8rem; color: var(--text-secondary);">
      &copy; <%= new Date().getFullYear() %> Microsoft Student Partners
    </div>
  </div>

  <script src="/background.js"></script>
</body>
</html>
