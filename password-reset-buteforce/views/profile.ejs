<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/style.css" />
  <title>Account</title>
</head>
<body>
  <div class="card">
    <h1>Your account</h1>
    <p>Signed in as <strong><%= user.username %></strong></p>
    <% if (user.isAdmin) { %>
      <div class="alert">Flag: <strong><%= user.flag %></strong></div>
    <% } else { %>
      <div class="alert">No admin-only data available for this account.</div>
    <% } %>
    <nav>
      <form action="/logout" method="POST">
        <button type="submit">Logout</button>
      </form>
      <a class="link-inline" href="/">Home</a>
    </nav>
  </div>
</body>
</html>
